<p>Posted a picture.</p>
<% if activity.targetable %>
<%= link_to image_tag(activity.targetable.asset.url(:thumb), class: ''), album_picture_path(act    ivity.user.profile_name, activity.targetable.album_id, activity.targetable_id) %>
<% end %>